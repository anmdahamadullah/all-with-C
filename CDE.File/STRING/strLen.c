#include <stdio.h>
#include <string.h>
int main() {

  char a[] = "Bangladesh is My own countru";
  int I = strlen(a);
  // Strlane never returns to Null character.

  printf("Your string is: %d \n ", I);

  return 0;
};